import 'package:flutter/material.dart';

class EventToDoList extends StatelessWidget {
  const EventToDoList({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}